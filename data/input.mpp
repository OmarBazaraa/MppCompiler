
int func(int x, char y, float z) {
	return 1;
}

int main() {

	int x;
	
	func(5.5, (x = 7)++, 5.5);

    switch (x) {
	default:
        x++;
    case 5:
    case 6:
        int y = 1;
        if (y > 0) {
            break;
        }
    case 7:
        switch (y)
            case 4:
            case 1100:
                break;
                int x;
    }

	// (((++x) = y++) = 7) = (z = 8);
}