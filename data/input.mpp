// Line comment
const int MAX = 10;

/**
 * Block comment
 */
int solve(int idx, int prv) {
    if (idx > MAX)
        return false;

    int n;

    for (int i = 0; i < n; ++i) {
        for (int j;;) {
            break;
        }
        continue;
    }

    return ((idx + prv) * 5) & solve(idx + 1, prv ^ ~23523);
}

void func() {

}

int main() {
    const int x = 5;

    int a;
    int b;

    (func * 6) + ((func() - 3) ^ 7);

    (7 = b);
    a = b = solve = 6;
    7 = b = solve = 6;
    (7 = b) = (solve = 6);

    if (x)
        solve(true, 'c');
    else if (0) {
        return 1;
    }

    switch (x)
    {
        case 5:
            solve(1,1);
            break;
    
        default:
            break;
    }

    9 + 1;
}